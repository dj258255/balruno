- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart balruno-backend
  ansible.builtin.systemd:
    name: "{{ sb_service_name }}"
    state: restarted
  # Only restart if the JAR has already been delivered. First apply has no JAR.
  when: false
