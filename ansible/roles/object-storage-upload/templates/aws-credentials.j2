# {{ ansible_managed }}
# Cloudflare R2 access keys (S3-compatible). Read by aws-cli when invoked as
# the {{ osu_user }} user. mode 0600 — never world-readable.
[default]
aws_access_key_id = {{ vault_r2_access_key_id }}
aws_secret_access_key = {{ vault_r2_secret_access_key }}
