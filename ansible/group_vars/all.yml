# Balruno Ansible 공통 변수 (전 머신)
# 시크릿은 group_vars/vault.yml 에 (ansible-vault 암호화)
#
# 결정 진실원: docs/backend/decisions/0007-infrastructure.md v1.1

# ─── OCI 4대 Public IP (vault 에서 override 가능) ─────────────────────────
prod_app_public_ip: "168.107.47.33"
monitor_public_ip:  "168.107.10.100"
backup_public_ip:   "134.185.108.159"
status_public_ip:   "158.179.162.44"

# ─── OCI 4대 Private IP (같은 VCN 10.0.0.0/24) ───────────────────────────
prod_app_private_ip: "10.0.0.11"
monitor_private_ip:  "10.0.0.248"
backup_private_ip:   "10.0.0.81"
status_private_ip:   "10.0.0.182"

# ─── SSH ──────────────────────────────────────────────────────────────────
ssh_user_arm: "rocky"          # OCI Rocky Linux 9 ARM 기본 user
ssh_user_x86: "rocky"          # OCI Rocky Linux 9 x86 기본 user
ssh_key_path: "~/.ssh/oci_key" # 사용자 통합 key

# ─── 도메인 ──────────────────────────────────────────────────────────────
domain_root: "balruno.com"
api_domain: "api.balruno.com"
collab_domain: "collab.balruno.com"
status_domain: "status.balruno.com"
monitor_domain: "monitor.balruno.com"

# ─── 버전 ────────────────────────────────────────────────────────────────
postgres_version: "17"
java_version: "25"
spring_boot_version: "4.1.0"
node_version: "22"             # Hocuspocus
nginx_version: "1.26"
grafana_version: "11.4.0"
loki_version: "3.3.0"
alloy_version: "1.5.0"
prometheus_version: "3.0.0"
alertmanager_version: "0.27.0"
influxdb_version: "2.7.10"
uptime_kuma_version: "1.23.16"
cloudflared_version: "latest"

# ─── 백업 (3-2-1 rule, ADR 0007 v1.1 §3.5) ─────────────────────────────
backup_retention_days: 7        # backup 머신 disk
object_storage_retention_days: 30
backup_cron_hour: 3             # 매일 새벽 3시 (monitor → backup rsync)
object_storage_cron_hour: 5     # 매일 새벽 5시 (backup → status → Object Storage upload)
oci_object_storage_region: "ap-seoul-1"  # Stage 0. Stage 2+ paying user 트리거 시 cross-region
oci_object_storage_bucket: "balruno-backup"

# ─── JVM / PG fail-safe 기본값 (Stage 0, ADR 0007 v1.1 §3.8) ───────────
spring_boot_mem_limit: "2g"
spring_jvm_options: "-XX:MaxRAMPercentage=75 -XX:+UseContainerSupport"
postgres_shared_buffers: "1GB"
postgres_effective_cache_size: "3GB"
postgres_work_mem: "16MB"
postgres_maintenance_work_mem: "256MB"
postgres_max_connections: 100
hocuspocus_mem_limit: "512m"
nginx_mem_limit: "256m"

# ─── 모니터링 (Stage 0, ADR 0007 §3.1 monitoring stack) ─────────────────
prometheus_retention_days: 7
prometheus_scrape_interval: "15s"
loki_retention_period: "168h"  # 7d
loki_ingestion_rate_mb: 4

# ─── Cloudflare ─────────────────────────────────────────────────────────
cloudflare_proxy_enabled: true
# Tunnel/Access 시크릿은 vault.yml

# ─── Stage (ADR 0010 — 점진 진화 트리거) ────────────────────────────────
current_stage: 0  # 0: 베타 0~10명, 1: 50~100명, 2: paying user, 3: 500+, 4: 5K+
