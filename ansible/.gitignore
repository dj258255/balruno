# Ansible vault password (plaintext — never commit).
# group_vars/vault.yml itself IS committed because it's encrypted with this password.
.vault_pass

# Ansible runtime artifacts
*.retry
.ansible/

# Local overrides (not in CI flow)
group_vars/local.yml
inventory.local.yml
