# English locale resource bundle. Identical to the default bundle
# (messages.properties) so an Accept-Language: en client and a
# missing-header client see the same strings.

duplicate.suffix= (copy)
starter.firstProject=My First Game
