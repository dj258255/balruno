{
  "name": "@balruno/shared",
  "version": "0.1.0",
  "private": true,
  "description": "플랫폼 비의존 공유 코드 — web/desktop/(미래 mobile). types + 게임 도메인 로직 (수식, 시뮬, 분석).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts",
    "./lib/*": "./src/lib/*.ts"
  },
  "scripts": {
    "lint": "echo 'shared lint TBD'",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
