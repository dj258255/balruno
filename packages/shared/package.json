{
  "name": "@balruno/shared",
  "version": "0.1.0",
  "private": true,
  "description": "web ↔ desktop ↔ (미래 mobile) 공유 코드. lib/ 와 types/ 중 플랫폼 비의존 부분을 점진적으로 추출. Phase F-2 (Electron) 시작 시점에 본격 추출 예정.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'shared placeholder — no lint yet'",
    "test": "echo 'shared placeholder — no tests yet'",
    "typecheck": "echo 'shared placeholder — no typecheck yet'"
  }
}
